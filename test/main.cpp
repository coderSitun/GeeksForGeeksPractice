#include<iostream>

using namespace std;

int main(){
    int testCaseCount;
    cin >> testCaseCount;
    string baseType, subType;
    while(testCaseCount--){
        cin >> baseType >> subType;
    }
    return 0;
}